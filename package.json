{
  "name": "turborepo-basic-shared",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --token=\"123\" --team=\"team-test-repo\"",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint --token=\"123\" --team=\"team-test-repo\"",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@types/node": "^17.0.7",
    "prettier": "^2.6.2",
    "turbo": "1.3.1"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  }
}
